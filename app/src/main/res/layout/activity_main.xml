<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#ff26b9ff"
        android:padding="10dp"
        android:id="@+id/mainTitleBar"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bookmarks"
            android:textSize="30sp"
            android:textColor="#ffffffff"
            android:paddingRight="30dp"/>

        <ImageButton
            android:layout_width="50dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:id="@+id/addCurrentLocationButton"
            android:src="@drawable/current_location"
            android:background="#00000000"
            android:onClick="addCurrentLocation"/>

        <ImageButton
            android:layout_width="50dp"
            android:layout_height="40dp"
            android:layout_toLeftOf="@id/addCurrentLocationButton"
            android:src="@drawable/new_location"
            android:background="#00000000"
            android:paddingRight="10dp"
            android:onClick="addNewLocation"/>



        </RelativeLayout>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:dividerHeight="2dp"
        android:divider="#ff26b9ff"
        android:id="@+id/mainListView"
        android:layout_below="@id/mainTitleBar"/>

</RelativeLayout>
